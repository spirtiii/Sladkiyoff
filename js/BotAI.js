import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.160.1/build/three.module.js';
export class BotAI { constructor(scene,position){this.group=new THREE.Group();this.hp=100;this.state='patrol';this.timer=.3+Math.random()*.2;this.shootAt=0;this.cover=new THREE.Vector3();const cloth=new THREE.MeshStandardMaterial({color:0x465654,roughness:.8}),skin=new THREE.MeshStandardMaterial({color:0xb67c58,roughness:.75});const body=new THREE.Mesh(new THREE.CapsuleGeometry(.23,.55,4),cloth);body.position.y=.7;const head=new THREE.Mesh(new THREE.SphereGeometry(.18),skin);head.position.y=1.25;head.userData.head=true;this.group.add(body,head);this.group.position.copy(position);scene.add(this.group); }
 update(dt,player,canMove){const d=this.group.position.distanceTo(player.position);this.timer-=dt;this.group.lookAt(player.position.x,this.group.position.y,player.position.z);if(d<18&&this.state==='patrol'){this.state='detect';this.timer=.3;}if(this.timer<=0){if(this.state==='detect'){this.state='attack';this.timer=.6;}else if(this.state==='attack'){this.state='reload';this.timer=1.1;}else{this.state='patrol';this.timer=1.5+Math.random()*2;}}
  if(this.state==='attack'&&canMove){const side=new THREE.Vector3(Math.sin(performance.now()*.003+this.group.position.x),0,Math.cos(performance.now()*.003+this.group.position.z)).multiplyScalar(dt*1.2);this.group.position.add(side);}return this.state==='attack'&&d<16&&Math.random()<dt*.72; }
 damage(n){this.hp-=n;return this.hp<=0;} }
